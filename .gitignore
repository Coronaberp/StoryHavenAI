venv/
__pycache__/
*.pyc

# Real user data — never commit the live database or its backups
storyhavenai.db
storyhavenai.db-shm
storyhavenai.db-wal
storyhavenai.db.backup*
*.logs.jsonl
*.log

# Contains real account credentials — never commit
CLAUDE.md
.claude/

# Retired pre-Postgres SQLite database + backups (dead weight, kept locally only)
legacy/

# User-uploaded content
media/

# Local scratch
files/

# StoryHaven AI installer-generated files (contain secrets)
.env
.env.*
docker-compose.yml
installer/Output/
backup_before_encrypt_backfill.sql
.superpowers/
storyhavenai.gputemp*
storyhavenai.gputemp*
test.html
